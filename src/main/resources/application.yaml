spring:
  application:
    name: mboard
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:file:./data/mboard
    driver-class-name: org.h2.Driver
    username: admin
    password: br12
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  session:
    store-type: none
server:
  servlet:
    session:
      timeout: 1200
    cookie:
      name: M_BOARD
      http-only: true
      secure: false